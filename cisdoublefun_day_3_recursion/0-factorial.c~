/*function that returns the factorial of a given number*/
int factorial(int n)
{
  int copy;
 
  copy = n-1;
  if(n < 0)
    {
      return -1;
    }
 else if(n == 0 || n == 1)
    {
      return 1;
    }

  while(copy >= 1)
    {
      n = n * copy;
      copy--; 
    }
  return n;
}
