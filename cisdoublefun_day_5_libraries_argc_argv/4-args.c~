#include <stdio.h>
/*prints all arguments it receives, starting from the last one*/
int main(int argc, char *argv[])
{
  int i;
  i = 0;
  while(i < argc)
    {
      printf("%s\n", argv[argc--]);
	     i++;
    }
	return 0;
}
